# MongoDB - use local or Atlas connection string
MONGODB_URI=mongodb://localhost:27017/pledge-dashboard

# JWT secret for authentication (use a strong random string in production)
JWT_SECRET=your_jwt_secret_here

# Server port (5001 avoids conflict with macOS AirPlay on 5000)
PORT=5001
